# SlideGrid Studio Handoff Document

## 1. 项目概述
- **项目名称**: SlideGrid Studio
- **项目类型**: 幻灯片/数字杂志排版编辑器 (Presentation/Magazine Slide Layout Editor)
- **技术栈**: React, TypeScript, Vite, TailwindCSS, Zustand, Framer Motion
- **前端包管理器**: `pnpm` (强制使用，禁止 npm/yarn)
- **前端目录**: `C:\Users\VerNe\Downloads\Documents\slide-layout-editor`
- **项目说明**: 一个基于区块和模板的、高度注重排版美学与几何构图的幻灯片布局编辑器，支持丰富的多比例（16:9, 2:3, A4 等）杂志级排版。

## 2. 本次会话完成的工作
- **目标**: 设计并精修 `Vertical Column` 和 `Horizon Sky` 两个全新的 2:3 高审美艺术排版模板。

### Git 节点记录
| Commit Hash | 描述 | 关键改动 |
| :--- | :--- | :--- |
| `未提交(Pending)` | feat: add Vertical Column and Horizon Sky templates | 添加并反复精修了 Vertical Column 的三列垂直排版和左右镜像功能；添加并精修了 Horizon Sky 的顶部留白与地平线标签对称设计。 |
| `a362d04` | feat: add Artistic L-Space, Floating Gallery, and Cinematic Letterbox templates and fix port conflict | 本次会话早些时候提交，添加了前三个 2:3 模板并推送至 GitHub。 |

### 具体改动清单
1. **新增模板 1：Vertical Column (垂直边栏)**
   - **文件**: `src/components/templates/VerticalColumn.tsx`, `src/templates/registry.ts`, `src/components/Preview.tsx`
   - **改动**: 新建组件。65% 出血大图，35% 侧边栏。经历了三次重构：
     1. 从普通的横向/底部文字布局，重构为 **三位一体的垂直排版 (Triple Vertical Layout)**：`[段落] | [副标题] | [主标题]`。
     2. 为段落文本 (`displayParagraph`) 引入了原生 CSS `[writing-mode:vertical-rl] [text-orientation:upright]`，并设定 `max-h-[70%]` 以支持长文本的自动垂直换行分列。
     3. 引入了随副标题高度自适应拉伸的垂直分割线（`w-[0.5px]`），并在上下两端留出半个字母 (`0.4rem`) 的留白，保证了几何关联性。
     4. 所有三列垂直文字设置为上端对齐 (`items-start`)，并做微小的 padding-top 补偿以保证视觉上的绝对水平。
     5. 添加了 `layoutVariant` 支持（Image Side: Left/Right），通过 `flex-row-reverse` 实现了一键左右镜像切换，并同步反转了内阴影方向（`shadow-[20px_0_60px_rgba(0,0,0,0.03)]` / `shadow-[-20px_0_60px_rgba(0,0,0,0.03)]`）和 Image Label 的位置（`left-16` / `right-16`）。
   - **字号调整**: 提升了整体可读性。Paragraph 从 `0.55rem` 升至 `0.75rem`，Subtitle 从 `0.6rem` 升至 `0.8rem`。

2. **新增模板 2：Horizon Sky (地平线天空)**
   - **文件**: `src/components/templates/HorizonSky.tsx`, `src/templates/registry.ts`, `src/components/Preview.tsx`
   - **改动**: 新建组件。上半部分保留 50% 巨大纯白“天空”负空间，下半部分“大地”放置居中贴底的正方形图片（70% 宽度）。
   - **排版结构**: 在文字区域实现了 `Top Teaser` -> `Sky Headline` -> `Detailed Verse` 的中轴线三段式布局。字号参考了 Vertical Column（Teaser 0.8rem, Verse 0.75rem, Headline 2.4rem）。
   - **地平线分割**: 在图片上方拉起了一条极细横线（`h-[0.5px]`）。
   - **Image Label 悬浮**: 将全局标注 (`imageLabel`) 从右下角移出，放置在“地平线”正上方，间距 (`mb-6`) 与下方图片间距 (`pt-6`) 形成完美的镜像对称。

3. **类型定义修复**
   - **文件**: `src/types.ts`
   - **改动**: 在 `PageData` 接口中补全了 `counterColor?: string;` 的定义。
   - **原因**: 修复了多个模板中由于 TypeScript 无法识别 `page.counterColor` 而产生的类型报错。

4. **注册表类型与逻辑修复**
   - **文件**: `src/templates/registry.ts`
   - **改动**: 
     1. 修复了 `Vertical Column` 中 `variant` 字段由于使用了非法的 `FieldType.SELECT` 而导致的运行时/编译错误。改为使用正确的 `props: { options: [...] }` 结构。
     2. 严格限制了新增的艺术模板仅在 `supportedRatios: ['2:3']` 比例下可用，撤销了之前误加的 16:9 支持，保持设计的专一性。

### 已修改文件列表
| 文件路径 | 改动摘要 |
| :--- | :--- |
| `src/types.ts` | 补全 `PageData.counterColor` 类型定义 |
| `src/components/Preview.tsx` | 注册渲染了 VerticalColumn 和 HorizonSky 组件 |
| `src/templates/registry.ts` | 导入并配置了两个新模板，修复了 FieldSchema 类型错误 |
| `src/components/templates/VerticalColumn.tsx` | 新增并经历了三次精修，支持三列垂直排版、自动换行和左右镜像 |
| `src/components/templates/HorizonSky.tsx` | 新增并精修，支持对称的地平线悬浮 Label 和三层式居中排版 |

## 3. 尚未完成的工作
- **[高] 提交未保存的 Git 变更**: `VerticalColumn.tsx` 和 `HorizonSky.tsx` 的创建，以及类型定义的修复尚未 commit。需要执行 `git add .` 和 `git commit -m "feat: add Vertical Column and Horizon Sky 2:3 templates"`。
- **[低] JSON Schema 引擎迁移**: 当前项目似乎正处于向 `JsonTemplateRenderer` 迁移的过程中。新增的这 2 个模板目前仍然使用传统的 React Component 硬编码实现（Hybrid 模式），未来重构时需要考虑如何用 Schema 定义复杂的 CSS 属性如 `writing-mode:vertical-rl`。

## 4. 设计规范参考
- **全局元数据动态绑定**:
  - `Vertical Column` 中的 `imageLabel` 实现了基于 `layoutVariant` 的动态 `left-16` / `right-16` 定位，与 `MetadataOverlay.tsx` 的全局页码完美对齐。
  - `Horizon Sky` 打破了常规，将 `imageLabel` 上提至视觉地平线处，这要求在设计具有明显横向分割线的模板时，标注可以脱离绝对的角落定位，转而服务于局部的几何对称（`mb-6` 对应 `pt-6`）。
- **CSS 垂直排版最佳实践**: 
  - 弃用了不稳定的 Flex 单字符堆叠，正式引入了原生 `[writing-mode:vertical-rl] [text-orientation:upright]` 用于长段落。
  - 必须配合 `max-h-[X%]` 使用，以触发文本在达到容器底部时的自动向左折行。
- **微调对齐 (Optical Alignment)**: 在 Flex `items-start` 中，即使所有元素上端对齐，不同字号的行高也会导致视觉上的参差不齐。使用 `pt-1` 或 `pt-0.5` 等微小内边距进行手动光学补偿是必要的。

## 5. 操作指引
- **如何继续**: 
  1. 首先执行 `git status` 确认哪些文件还没提交，并提交本次的改动。
  2. 如果需要继续修改模板或新增模板，请遵循先修改 `components/templates`，再更新 `templates/registry.ts`，最后注册进 `Preview.tsx` 的流程。
- **如何回滚**: 若需要回退最近一次的修改（撤销未提交的改动）：`git reset --hard HEAD`。
- **项目启动**: 在工作区根目录执行 `pnpm dev` 即可，服务运行在 `localhost:13100`。
- **注意事项**: 
  - 本项目**强制使用 `pnpm`**，绝对不可使用 `npm install`。
  - 新增模板时，如果涉及特殊的布局方向，请利用 `page.layoutVariant` 配合 Flex 的 `flex-row-reverse` 实现免去写两套 CSS 的方案。
